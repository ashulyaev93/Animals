package Animals;

public class Cat extends Animals
{
        //кот не умеет плавать
        public Cat(String name, int run, int jump)
        {
                this.name = name;
                this.run = run;
                this.jump = jump;
        }
}
